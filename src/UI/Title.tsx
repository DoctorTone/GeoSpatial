import Typography from "@mui/material/Typography";

const Title = () => {
  return (
    <div id="title" className="panel">
      <Typography variant="h3">Lightning</Typography>
    </div>
  );
};

export default Title;
